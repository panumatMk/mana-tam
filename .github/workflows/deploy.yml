name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master # หรือ main ขึ้นอยู่กับชื่อ branch หลักของคุณ

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20 # 🔥 FIX: เปลี่ยนเป็น 20 หรือ 22 เพื่อรองรับ Vite และ Rolldown
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build project (npm run build)
        run: npm run build

      # 💡 Deploy to gh-pages branch
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          # ตัวนี้ต้องมีสิทธิ์ Write Access
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
